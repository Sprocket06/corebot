const CommandManager = require('../commandManager.js')
const Config = require('../config.json')
const { argParse } = require('../utils.js')

CommandManager.addHandler('!info', (args,msg)=>{

})

CommandManager.addHandler('!addInfo', (args,msg)=>{
  //Usage: !addInfo name:Mana Magnet text: Text follows kekw

})

CommandManager.addHandler('!editInfo', (args,msg)=>{

})
